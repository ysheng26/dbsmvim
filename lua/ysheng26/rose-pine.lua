require("rose-pine").setup({
    -- dim_inactive_windows = true,
    styles = {
        bold = false,
        italic = false,
        transparency = false,
    }
})

vim.cmd("colorscheme rose-pine-moon")
